# MMORPG 서버 부하테스트 — 측정 방식 상세

> 이 문서는 "서버 연산을 얼마나 효율적으로 개선하는가"를 **숫자로 증명**하기 위한 측정 체계 전체를 설명한다.
> 대상 프로젝트: `MMORPG_Project` (IOCP 게임서버 + Direct2D 클라이언트). 측정은 서버 **Release** 빌드 기준.
> 최종 갱신: 2026-07-23

---

## 1. 이 측정이 답하려는 질문

포트폴리오의 목표는 **"최대 동접 몇 명"이 아니라 "서버의 핵심 연산(AOI·길찾기)을 알고리즘으로 얼마나 개선했는가"**이다.

그래서 측정의 최종 산출물은 다음 형태의 **개선 전/후 비교표**다.

| 지표 | 개선 전 | 개선 후 |
|---|---|---|
| 같은 부하(예: 한 존 2000명)에서 특정 함수가 먹는 CPU 코어 수 | (측정값) | (측정값) |
| 서버 패킷 처리 지연 p99 | (측정값) | (측정값) |

이 표를 만들려면 "**부하를 고정한 채 서버 내부 비용을 재는**" 측정 도구가 필요하다. 이 문서는 그 도구가 어떻게 구성돼 있고 어떻게 돌리는지를 설명한다.

---

## 2. 왜 "최대 동접"이 아니라 "서버 내부 계측"인가 (중요한 전제)

처음에는 "봇을 계속 늘려 서버가 못 버티는 지점 = 최대 동접"을 재려 했다. 하지만 우리 환경(**서버 1대 + 부하 봇 1대**)에서는 이 방식이 **원리적으로 불가능**하다는 걸 확인했다.

- **부하 봇이 서버보다 먼저 뻗는다.** 봇 한 프로세스가 존 전체의 브로드캐스트(플레이어 수에 비례 ∝ N, 그것이 N명에게 전달되므로 전체 ∝ N²)를 **통째로 수신**한다. 약 3600~4200봇 지점에서 봇의 종단 지연이 20초 이상으로 폭발하는데, **그때 서버는 p99 8ms로 멀쩡**했다.
- **localhost 함정.** 서버와 봇을 같은 PC에서 돌리면 둘이 같은 CPU를 두고 싸워서 더 빨리 멜트다운한다. 즉 이때의 붕괴는 "서버 한계"가 아니라 "측정 도구 + CPU 경합"의 한계다.
- **16코어 서버를 노트북 한 대 부하로 포화시키려면** 1만~2만 동접이 필요한데, 봇 한 대로는 그 부하를 못 만든다.

### 결론 — 전략 전환

> **최대 동접 숫자를 쫓는 대신, 병목 함수(`GetNearPlayers` 등)를 서버 내부에서 직접 계측한다.**
> 서버를 포화시키지 않고도 "이 함수가 N명에서 CPU를 얼마나 먹는가 / 어떻게 증가하는가(O(N²))"를 수치화할 수 있고, 이게 알고리즘 개선의 before/after를 증명하는 데 훨씬 강력하다.

- **서버 콘솔의 p99 = 서버의 진실** (순수 처리 비용).
- **봇 콘솔의 지연 = 부하 봇의 건강 상태** (봇이 뻗으면 이 값이 뻥튀기됨 — 서버 성능과 무관).

이 두 개를 **항상 같이** 본다. 봇 지연이 폭발했는데 서버 p99가 낮으면 → 병목은 봇(측정 도구)이지 서버가 아니다.

---

## 3. 측정 지표 정의

### 3.1 p50 / p99 (백분위 지연)

측정한 지연 값들을 **작은 순서로 정렬**했을 때 특정 위치의 값.

- **p50 (중앙값)**: "요청의 절반은 이 값보다 빨랐다" = 평소 상태.
- **p99**: "요청의 99%는 이 값보다 빨랐다. 최악의 1%만 이보다 느렸다" = 최악의 사용자 경험.

**평균(avg)을 쓰지 않는 이유**: 값 하나가 튀면(800ms 렉 한 번) 평균이 크게 흔들려 진실을 가린다. 예를 들어 `[5,5,6,5,6,5,5,6,5,800]`은 평균 84.8ms지만 p50은 5ms, p99는 800ms다 → "평소엔 빠른데 가끔 한 명이 심하게 렉 먹는다"는 진짜 상황이 p50/p99로만 드러난다. MMO에서는 이 **꼬리 지연(tail latency)**이 곧 특정 플레이어의 게임 불능을 뜻하므로 p99가 핵심이다.

### 3.2 두 종류의 지연 (반드시 구분)

| 지표 | 측정 위치 | 무엇을 재나 | 포함하는 것 |
|---|---|---|---|
| **서버 처리 지연** (`[부하 계측]` p50/p99) | 서버 | 패킷 1건을 핸들러가 처리하는 순수 시간 | 락 대기 + `GetNearPlayers` + 길찾기 등 서버 연산만 |
| **봇 종단 지연** (봇 콘솔 `지연`) | 봇 | 이동→서버 처리→내 시야로 브로드캐스트→수신까지 전체 파이프라인 | 네트워크 왕복 + 서버 큐 대기 + 서버 처리 전부 |

### 3.3 AOI 계측 (`GetNearPlayers` 전용)

이번 프로젝트의 **핵심 병목 지표**. 아래 4개를 본다.

- **호출/s**: `GetNearPlayers`가 초당 몇 번 불리는가. (플레이어 수에 비례 ∝ N)
- **평균순회 N**: 1회 호출에서 순회한 플레이어 수. 지금 구현은 **존 전체를 순회**하므로 이 값이 접속 수와 같다 = O(N) 스캔의 증거.
- **1회 us / 총 ms/s / CPU %코어**: 한 번 호출에 걸린 시간 / 초당 이 함수에 쓴 총 시간 / 그게 코어 몇 개분인가.
- **max us**: 단일 호출 최악값.

### 3.4 기타

- **패킷 처리량 (pps)**: 서버가 초당 처리한 패킷 수.
- **워커별 처리 건수**: IOCP 워커 스레드별 처리량 → 부하가 코어에 고르게 분산되는지 확인.

---

## 4. 측정 아키텍처 (코드가 어떻게 짜여 있나)

### 4.1 `StressMetrics.h` — 헤더 온리 계측 코어

경로: `MMO_GameServer/MMO_GameServer/StressMetrics.h`

- **원자(atomic) 카운터 + 고정 크기 히스토그램**으로 락·힙할당 없이 계측한다.
- 시간은 `QueryPerformanceCounter`(QPC)로 마이크로초(us) 단위 측정. 콜당 QPC 2회 + relaxed atomic 몇 번(수십 ns) → **측정 자체의 오버헤드가 무시할 수준**.
- 히스토그램 버킷은 **2의 거듭제곱 us 버킷** 32개(`buckets[i] = [2^i, 2^(i+1)) us`). 매 패킷 정렬하면 느리므로 히스토그램으로 p50/p99를 근사(표준 기법).
- 스냅샷 시 버킷을 `exchange(0)` → 항상 **"직전 구간"** 통계가 된다.

핵심 함수:

| 함수 | 역할 |
|---|---|
| `Now()` / `ElapsedUs(start)` | QPC 시각 / 경과 us |
| `Record(us)` | 패킷 처리 1건 기록(버킷+합+워커별) |
| `RecordAoi(us, nScanned)` | `GetNearPlayers` 1회 기록(시간+순회수) |
| `SnapshotAndReset()` | 패킷 처리 통계 스냅샷(p50/p99/max/워커별) + 리셋 |
| `SnapshotAoiAndReset()` | AOI 통계 스냅샷 + 리셋 |

### 4.2 패킷 처리 시간 계측 — `Packet_Handler.cpp`

모든 패킷 핸들러를 감싸서 처리 시간을 기록한다.

```cpp
// CPacket_Handler::Handle 진입/종료
const int64_t nMetricStart = StressMetrics::Now();   // 진입
// ... 실제 패킷 디스패치(락, GetNearPlayers, 길찾기 전부 포함) ...
StressMetrics::Record(StressMetrics::ElapsedUs(nMetricStart));  // 종료
```

→ 이 시간에는 이동 처리 중 부르는 `GetNearPlayers`·A* 등 **서버 연산이 전부 포함**된다. 그래서 병목이 커지면 이 p99가 올라간다.

### 4.3 AOI 계측 — `Zone.cpp::GetNearPlayers`

```cpp
std::vector<int32_t> CZone::GetNearPlayers(PlayerRef pPlayer)
{
    const int64_t nAoiT0 = StressMetrics::Now();
    std::vector<int32_t> vResult;
    uint32_t nScanned = 0;
    {
        std::lock_guard<std::mutex> lock(m_zoneLock);   // ← 전역 존 락
        nScanned = (uint32_t)m_playerIDs.size();
        for (int32_t nID : m_playerIDs)                 // ← 존 전체 O(N) 순회
        {
            if (nID == pPlayer->m_nPlayerID) continue;
            PlayerRef pOther = ...Get_Player(nID);
            if (pOther && CanSee(pPlayer, pOther))       // 타일 거리 <= VIEW_RANGE(5)
                vResult.push_back(nID);
        }
    }
    StressMetrics::RecordAoi(StressMetrics::ElapsedUs(nAoiT0), nScanned);
    return vResult;
}
```

- 매 호출 **존의 모든 플레이어 N명을 순회** → O(N).
- 플레이어가 타일을 넘을 때마다 호출되므로 호출 횟수도 ∝ N → **전체 비용 ∝ N²**.
- 전체가 하나의 `m_zoneLock` 안에서 도므로 **모든 AOI 호출이 직렬화**된다(멀티코어를 못 씀).

### 4.4 콘솔 출력 — `IOCP_Server.cpp::DebugConsoleThread`

500ms마다 스냅샷을 떠서 서버 콘솔에 출력한다. 출력 형식은 §6 참고.

---

## 5. 부하 봇 (`MMO_StressTest`) 구조

경로: `MMO_StressTest/MMO_StressTest/StressBot.cpp`. **실제 클라이언트와 동일한 전송 규약을 쓰는 IOCP 클라이언트**다.

### 5.1 스레드 구조 (중요)

| 스레드 | 역할 |
|---|---|
| **ConnectorThread** | 접속(블로킹 connect) 전담 |
| **ControlThread** | 30ms마다 전 봇의 이동·전투 구동 |
| **WorkerThread × 코어수** | IOCP 수신 완료 처리(패킷 파싱·통계 집계) |

> connect(블로킹)와 이동 구동을 **분리**한 이유: 예전엔 한 스레드가 둘을 같이 해서 고부하 시 ~3500에서 정체했다. 분리 후 4700+까지 접속됨.

### 5.2 이동/전투 모델 (실제 클라와 동일해야 서버 검증 통과)

- 이동 속도 **1타일/초** (서버 해킹 방지 허용오차 2타일 안).
- 목적지 변경 → `CS_MOVE_DEST`, **타일이 바뀔 때만** → `CS_MOVE_POS`(타이머 아님).
- 스폰(홈) 중심 `WANDER`(15타일) 반경 배회 → 한 존에 봇을 몰아 밀도 유지.
- 근처 몬스터 있으면 다가가 `CS_ATTACK_MONSTER`, 죽으면 `CS_RESPAWN`.

### 5.3 봇 종단 지연 측정 원리

봇이 이동 패킷에 `moveTime = GetTickCount64()`를 찍어 보내면, 서버가 `SC_MOVE_PLAYER` 브로드캐스트에 그대로 실어 돌려준다. 수신한 봇이 `지연 = 지금 - moveTime`으로 계산한다. **모든 봇이 한 프로세스/한 시계**라 이 값이 유효하다(누가 움직이든 같은 시계). 고부하에서 원자변수 경합을 줄이려 **1/4만 샘플링**해도 통계는 충분히 정확하다.

### 5.4 hold vs ramp 모드

- **hold**: 목표 봇 수를 빠르게 채우고 **고정 유지** → 특정 부하에서의 정상상태 측정용. **(개선 전/후 비교는 이걸 씀)**
- **ramp**: 봇 종단 지연 임계까지 자동 증설 → 최대 동접 탐색용(우리 환경에선 봇이 먼저 뻗어 한계가 있음).

### 5.5 서버측 우회 (`STRESS_TEST` 빌드)

- 서버 `Handle_CS_LOGIN`의 `#ifdef STRESS_TEST` 분기: id가 `bot_*`면 **DB 조회를 스킵**하고 입장.
- **비밀번호(pw) = 존 번호**. 재빌드 없이 원하는 존으로 봇을 넣는다 (평지 `ZONE_RAID_FLAT`=6 / 장애물 `ZONE_RAID`=5).
- 봇은 자동저장 대상에서 제외, 죽으면 랜덤 위치 부활(코너 뭉침 방지).

---

## 6. 서버 / 봇 콘솔 읽는 법 (실제 출력 주석)

### 6.1 서버 콘솔 (`[부하 계측]` + `[AOI GetNearPlayers]`)

```
[부하 계측]  최근 0.50s
  패킷 처리량 : 2749 pkt/s   (구간 1377건)
  처리 지연   : avg 2.6ms  p50 1.0ms  p99 8.2ms  max 12.6ms
  워커별 처리 : 78 89 91 85 91 75 92 99 83 83 85 86     ← 16워커에 고르게 분산
[AOI GetNearPlayers]
  호출 1661/s  평균순회 N=1500  1회 3093.0us  총 5137.8ms/s  (CPU 513.8%코어)  max 12396us
```

읽는 법:
- `처리 지연 p99 8.2ms` = 서버가 패킷 하나 처리하는 데 걸린 시간의 99분위. **이게 서버의 진짜 건강 지표.**
- `평균순회 N=1500` = 매 AOI 호출이 1500명을 다 훑고 있다 → O(N) 스캔 확정.
- `총 5137.8ms/s (CPU 513.8%코어)` = `GetNearPlayers` 한 함수가 **CPU 5.1코어**를 먹고 있다.

### 6.2 봇 콘솔

```
접속 1500/1500 | 송신 2742 수신 32197 pps 986.5 KB/s | 몬 0 공격 0 /s | 지연 avg 9.9 p50 15 p99 31 max 47 ms
```

읽는 법:
- `접속 1500/1500` = 실제 게임 입장(ENTER_GAME 수신) 완료 봇 수 / 목표.
- `수신 32197 pps` = 봇이 초당 받는 패킷(브로드캐스트) 수. 봇이 이걸 다 소화 못 하면 멜트다운.
- `지연 p99 31ms` = 봇 종단 지연. **이게 수백ms~수초로 튀면 봇이 뻗은 것**(서버 문제 아님).
- `몬 / 공격` = 몬스터 패킷·공격 pps. 전투가 도는지 확인용(몬스터 0으로 두면 순수 AOI 측정).

---

## 7. 재현 절차 (런북)

### 7.1 전제

- MySQL 구동 중(`MySQL80` 서비스). 서버가 `[DB] connected` 출력해야 정상.
- 서버 vcxproj에 `STRESS_TEST` 매크로 정의됨(Debug/Release x64). **측정은 Release.**
- 동적 포트 범위 16384개(49152~65535) — 봇 수천 개도 포트 고갈 없음.

### 7.2 빌드

```powershell
$msbuild = "C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe"
# 서버가 떠 있으면 링크 LNK1168 → 먼저 종료
Stop-Process -Name MMO_GameServer -Force -ErrorAction SilentlyContinue
& $msbuild ".\MMO_GameServer\MMO_GameServer.sln"   /t:Build /p:Configuration=Release /p:Platform=x64 /m /nologo /v:m
& $msbuild ".\MMO_StressTest\MMO_StressTest.sln"   /t:Build /p:Configuration=Release /p:Platform=x64 /m /nologo /v:m
```

산출물:
- `MMO_GameServer\x64\Release\MMO_GameServer.exe`
- `MMO_StressTest\x64\Release\MMO_StressTest.exe`

### 7.3 실행

```
MMO_StressTest.exe <서버IP> <봇수> <존번호> [hold|ramp]
# 예) 한 존 1500명 고정, 평지 존:
MMO_StressTest.exe 127.0.0.1 1500 6 hold
```

- 서버를 먼저 띄우고(포트 7777 리슨 확인) → 봇 실행 → 30~45초 정상상태 → 서버/봇 콘솔의 §6 라인 기록.

### 7.4 중요 주의 — 측정점마다 서버를 새로 띄울 것

봇을 **강제 종료하면 수천 개 소켓이 동시에 끊기고**, 서버가 그 대량 disconnect(+동시 DB 저장 폭주)를 정리하다 **크래시**한다(§9.2). 따라서 여러 봇 수를 스윕할 때는 **각 측정점마다 서버를 새로 시작**해야 다음 점이 오염되지 않는다.

### 7.5 측정 환경 조언

- **정확한 용량/지연 곡선**은 봇을 **다른 PC(노트북, 같은 LAN)**에서 돌려야 나온다(실제 네트워크 왕복 포함, CPU 경합 없음). 서버는 `INADDR_ANY`라 그대로 열린다.
- **localhost(한 PC)**는 "몇 명까지 붙나 / AOI 비용이 어떻게 커지나" 확인용. 최대 동접 판정에는 부적합.
- 몬스터 수는 환경변수 `STRESS_MON_COUNT`로 제어. **0으로 두면 순수 AOI**만 측정(전투 부하 배제).

---

## 8. 실측 결과 (2026-07-23, 개선 전 베이스라인)

**조건**: 한 존(RaidFlat=6) 밀집, 몬스터 0(순수 AOI), localhost 데스크탑 16코어, hold 모드.

| 봇 N | AOI 호출/s | 순회 N | **초당 스캔(호출×N)** | AOI CPU 코어 | 서버 p99 | 봇 p99 |
|---|---|---|---|---|---|---|
| 1000 | 1054 | 1000 | **105만** | 3.6 | 8.2ms | 32ms |
| 1500 | 1661 | 1500 | **249만** | 5.1 | 8.2ms | 31ms |
| 2000 | 2292 | 2000 | **458만** | 6.1 | 8.2ms | 32ms |

### 해석

1. **O(N²)가 숫자로 증명됨.** 봇 2배(1000→2000)에 초당 스캔 4.35배(105만→458만) ≈ 4배 = N². `GetNearPlayers`가 매 호출 전체 N명 순회 + 호출도 ∝N.
2. **이 함수 하나가 16코어 중 6코어를 소모** — 다른 모든 로직 합보다 큼. 병목 위치 특정 완료.
3. **CPU(시간)가 스캔만큼 4배로 안 뛰는 이유가 두 번째 병목.** 스캔은 4.35배인데 시간은 3.6→6.1코어(1.7배)뿐 → `m_zoneLock` **전역 락 직렬화**로 멀티코어를 못 쓰기 때문. 병목이 **이중**이다: ① O(N²) 연산량 ② 전역 락 직렬화.
4. **서버·봇 둘 다 이 부하에선 멀쩡**(서버 p99 8ms 고정, 봇 p99 32ms) → 소위 "4000 벽"은 서버 한계가 아니라 부하 봇 멜트다운 + localhost CPU 경합임을 재확인.

---

## 9. 알려진 한계 / 주의사항

### 9.1 부하 봇이 서버보다 먼저 뻗음
봇 한 프로세스가 존 전체 N² 브로드캐스트를 수신 → ~3600~4200에서 종단 지연 20초+ 멜트다운. 그때 서버는 p99 8ms. → 최대 동접 측정 불가, 서버 내부 계측으로 우회(§2).

### 9.2 대량 동시 disconnect 시 서버 크래시 (미해결 버그)
봇 ~1500개 동시 강제종료 → 세션 정리 폭포 + **끊기는 봇마다 DB 저장이 한꺼번에 몰림** → 서버 무음 종료(access violation류). 원인 의심: connection-per-call DB 저장이 워커를 블로킹/동시 폭주. **해결 방향**: 전용 DB 스레드 + 커넥션 풀(저장 큐잉). 측정 시에는 §7.4로 회피.

### 9.3 작은 맵에서는 섹터 분할 이득이 작음
시야(11×11=121칸)가 맵의 큰 비율이면 섹터를 쪼개도 이득이 적다. 그래서 **150×150 대형 맵**을 측정용으로 만들었다(시야가 맵의 ~0.5% → 섹터 효과 큼). 대신 큰 맵은 봇이 흩어져 부하가 안 걸리므로, 밀집 시나리오는 스폰 한 곳에 몰아넣어(`WANDER` 작게) 만든다.

### 9.4 측정 왜곡 제거 — SLOG
전투/AI 로그(`std::cout`)가 핸들러 안에서 돌면 콘솔 I/O 락이 p99를 부풀린다(실측: 로그 有 p99 16ms → 無 0.5ms). 그래서 게임 로직 로그는 `SLOG` 매크로로 감싸 STRESS_TEST 빌드에서 **무음** 처리한다. 통계 출력용 `std::cout`/`printf`만 남긴다.

---

## 10. 핵심 상수 / 설정 위치

| 항목 | 값 | 위치 |
|---|---|---|
| 서버 세션 상한 `MAX_SESSION` | 20000 | `Session_Manager.h` |
| 서버 플레이어 상한 `MAX_PLAYER` | 20000 | `Player_Manager.h` (세션ID로 인덱싱 → MAX_SESSION과 같이 올릴 것) |
| AcceptEx 풀 `ACCEPT_POOL_SIZE` | 64 | `IOCP_Server.h` |
| 시야 반경 `VIEW_RANGE` | 5 타일 | `Zone` (섹터 크기는 ≥5여야 3×3으로 시야를 덮음) |
| 봇 배열 상한 `MAX_BOTS` | 20000 | `StressBot.cpp` (서버 MAX_SESSION과 맞춤) |
| 봇 이동 구동 주기 `CONTROL_TICK_MS` | 30ms | `StressBot.cpp` |
| 몬스터 수 | env `STRESS_MON_COUNT` (0=순수 AOI) | 서버 `Zone_Manager` |

---

## 11. 다음 단계 — 섹터 AOI (개선안)

**목표**: `GetNearPlayers`의 O(N²) 전체 순회 + 전역 락 직렬화를, **공간 분할(섹터)**로 바꾼다.

- 존을 섹터 격자로 쪼개고(섹터 한 변 ≥ VIEW_RANGE=5), 플레이어가 타일 이동 시 섹터를 옮긴다.
- `GetNearPlayers`는 **자기 섹터 + 인접 3×3 섹터**만 순회 → 순회 대상이 전체 N이 아니라 "주변 밀도"로 제한.
- 섹터별 락으로 **직렬화 해소 → 멀티코어 활용**.

**검증 방법**: §8의 3점(1000/1500/2000)을 **같은 조건으로 재측정** → before/after 표 완성. 이게 포트폴리오의 하이라이트.

---

## 부록 A. 용어 빠른 정리

- **AOI (Area Of Interest)**: 플레이어가 봐야 하는 주변 범위. 여기선 시야(VIEW_RANGE=5) 안의 다른 플레이어를 찾는 것.
- **O(N²)**: 부하 N을 2배로 늘리면 비용이 4배가 되는 증가 곡선. 동접이 커질수록 급격히 나빠짐.
- **p50 / p99**: §3.1.
- **pps**: packets per second.
- **IOCP**: Windows의 고성능 비동기 I/O 모델(소수 워커 스레드로 수천 소켓 처리).
- **SLOG**: 부하 측정 시 게임 로직 로그를 끄는 스위치 매크로(§9.4).
